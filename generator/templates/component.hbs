import React, {PropTypes} from "react";
import Description from "../include/description";
import ExampleContainer from "../include/example-container";
import Snippet from "../include/code-snippet";
import TestResult from "../include/test-result";
{{#if exampleClassName}}import {{ exampleClassName }} from "./{{ exampleFileName }}";{{/if}}

export default class {{ componentClassName }} extends React.Component {
    render() {
        {{{ description }}}
        {{{ codeSnippet }}}

        return (
            <div>
                {{#if description}}
                <Description description={description} />
                {{/if}}
                {{#if codeSnippet}}
                <Snippet code={codeSnippet} />
                {{/if}}
                {{#if exampleUsage}}
                    <ExampleContainer>
                        {{{ exampleUsage }}}
                    </ExampleContainer>
                {{/if}}
            </div>
        );
    }
}