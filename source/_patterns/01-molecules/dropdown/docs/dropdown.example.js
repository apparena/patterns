import React from 'react';
import {Dropdown} from 'apparena-patterns-react';

export default function DropdownExample() {
    return (
        <Dropdown buttonSize="lg" arrow="true" items={[]}>
            <p>And this is one too</p>
        </Dropdown>
    );
}
