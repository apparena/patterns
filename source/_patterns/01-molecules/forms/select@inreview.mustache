{{# select }}
    <div class="{{ class }}">
        <select class="form-control {{ styleModifier }} " {{#message}}data-toggle="tooltip" data-placement="right" title="{{ message }}"{{/message}}>
            {{# options }}
                <option {{#selected}}selected="selected"{{/selected}} >{{text}}</option>
            {{/options}}

        </select>
    </div>
{{/select}}