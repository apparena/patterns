import React from 'react';
import {ReactComponent, TextArea} from 'apparena-patterns-react';

export default class MyAwesomeClass extends ReactComponent {
    render() {
        return (
            <TextArea placeholcer="I'm the placeholder"/>
        );
    }
}