import React, {PropTypes} from "react";
import {ReactComponent} from "apparena-patterns-react";
import Description from "include/description";
import ExampleContainer from "include/example-container";
import PropsContainer from "include/props-container";
import Snippet from "include/code-snippet";
import TestResult from "include/test-result";
{{#if exampleFileName}}import Example from "./docs/{{ exampleFileName }}";{{/if}}
{{#if propsFileName}}import PropsTable from "./docs/{{ propsFileName }}";{{/if}}

export default class {{ componentClassName }} extends ReactComponent {
    render() {
        {{{ description }}}
        {{{ codeSnippet }}}

        return (
            <div>
                {{#if description}}
                <Description description={description} />
                {{/if}}
                {{#if codeSnippet}}
                <Snippet code={codeSnippet} />
                {{/if}}
                {{#if exampleFileName}}
                <ExampleContainer>
                    <Example/>
                </ExampleContainer>
                {{/if}}
                {{#if propsFileName}}
                <PropsContainer>
                    <PropsTable/>
                </PropsContainer>
                {{/if}}
            </div>
        );
    }
}